# MR.NET — Live Events Website

This is a Next.js (App Router) + Tailwind site focused on a single use case: **Live Events**.

## Run locally
```bash
npm install
npm run dev
```

Open: http://localhost:3000
